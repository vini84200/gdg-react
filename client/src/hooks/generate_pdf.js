import { useState, useEffect, useRef, useCallback } from "react";

export function usePDF(templateStr, locals) {
  function generate(cb) {}

  function generateAndOpen() {}

  return { generate, generateAndOpen };
}
